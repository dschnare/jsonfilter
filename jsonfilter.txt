COMMAND DOCUMENTATION

    Jsonfilter implements a command line tool to filter string values found
    in JSON data. Use `jsonfilter --help` for details about each command
    line argument.

    This tool supports standard in piping of a JSON file contents. If the
    `--output` argument is not specified then output will be piped to
    standard out.

SUBDIRECTORIES

	filter

